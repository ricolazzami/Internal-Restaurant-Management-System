# Web App Developer Setup

## Local install

```bash
cd apps/web
npm install
```

## Start the dev server

```bash
cd apps/web
npm run dev
```

This starts the Vite dev server for the web app. Open the local URL shown in the terminal (usually http://localhost:5173).

## Run tests

```bash
cd apps/web
npm run test -- --run
```
